<header>
    <nav class="navbar navbar-light navbar-expand-md fixed-top">
        <div class="container">
            <a href="{{ path('home') }}" class="me-2">
                <img src="{{asset('images/static_assets/Homey-color.svg')}}" alt="Homey logo" height='50'>
            </a>
            <button
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                class="navbar-toggler"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation"
            >
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id='navbarNav'>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a 
                            class="nav-link {{ current_menu is defined and current_menu == 'properties' ? 'active' : '' }}" 
                            aria-current="page" href="{{ path('property.index') }}"> 
                            Acheter
                        </a>
                    </li>
                {% if app.user and is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a 
                            class="nav-link" 
                            aria-current="page" href="{{ path('admin.property.index') }}">
                            Administration
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a 
                            class="nav-link {{ current_menu is defined and current_menu == 'properties' ? 'active' : '' }}" 
                            aria-current="page" href="{{ path('login') }}">
                            Connexion
                        </a>
                    </li>
                {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</header>



{# <nav class="navbar navbar-expand-lg" style="background-color: #e3f2fd;">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">Mon agence</a>
        <button class="navbar-toggler"  type="button"  data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon" ></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
            <a class="nav-link {{ current_menu is defined and current_menu == 'properties' ? 'active' : '' }}" aria-current="page" href="{{ path('property.index') }}">Acheter</a>
            </li>
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link {{ current_menu is defined and current_menu == 'properties' ? 'active' : '' }}" aria-current="page" href="{{ path('admin.property.index') }}">Gérer les biens</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ current_menu is defined and current_menu == 'properties' ? 'active' : '' }}" aria-current="page" href="{{ path('admin.option.index') }}">Gérer les options</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ current_menu is defined and current_menu == 'properties' ? 'active' : '' }}" aria-current="page" href="{{ path('logout') }}">Déconnexion</a>
                </li>
            {% else %}
                <li class="nav-item">
                <a class="nav-link {{ current_menu is defined and current_menu == 'properties' ? 'active' : '' }}" aria-current="page" href="{{ path('login') }}">Connexion</a>
                </li>
            {% endif %}
            
        </div>
    </div>
</nav> #}