{% extends 'base.html.twig' %}

{% block title %}ImageTest{% endblock %}

{% block body %}
    <h1>ImageTest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_test.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ image_test.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_test_index') }}">back to list</a>

    <a href="{{ path('app_image_test_edit', {'id': image_test.id}) }}">edit</a>

    {{ include('image_test/_delete_form.html.twig') }}
{% endblock %}
